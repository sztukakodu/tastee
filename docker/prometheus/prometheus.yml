global:
  scrape_interval: 30s
  scrape_timeout: 10s

scrape_configs:
  - job_name: services
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          # IP in MacOS: ifconfig | grep "inet " | grep -v 127.0.0.1
          - '192.168.1.25:8080'
